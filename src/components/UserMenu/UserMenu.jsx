// import PropTypes from 'prop-types'

function UserMenu() {
  return (
    <div>
      <div>Почта</div>
      <button type="button">Выйти</button>
    </div>
  );
}

// UserMenu.propTypes = {
// }

export default UserMenu;
